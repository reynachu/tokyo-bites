<% if current_user == @user %>
<% elsif current_user.follows?(@user) %>
  <%= button_to "Unfollow", unfollow_user_path(@user), mtheod: :delete, class: 'btn btn-danger' %>
<% else %>
  <%= button_to "Follow", follow_user_path(@user), method: :post, class: 'btn btn-primary' %>
<% end %>
